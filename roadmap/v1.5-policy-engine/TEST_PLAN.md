# Test Plan — Version 1.5: Policy Engine & Admin Operational Console

- `test_approval_gate_blocks_unsupported_publication()`: Verify report publication blocked when required evidence missing.
- `test_admin_policy_crud()`: Verify policy rule creation and activation via `/admin/policies`.
